# Copy to .env.local and adjust. In dev the app talks to /api (proxied by Vite to the backend).
# Backend origin the Vite dev-server proxies /api to:
VITE_API_TARGET=http://localhost:8080
# Optional: absolute API base (leave empty to use the /api proxy path)
VITE_API_BASE=
